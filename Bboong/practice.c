#include <stdlib.h>
#include <time.h>
#include <stdio.h>

int main() {
	srand(time(NULL));
	int QuizNum = 1;
	
}

int Quiz(int QuizNum) {
	int First = rand() % 20 + 1;
	int Second = rand() % 20 + 1;
}